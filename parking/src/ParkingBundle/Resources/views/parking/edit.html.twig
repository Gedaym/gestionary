{% extends 'base.html.twig' %}

{% block body %}
    <h1>Parking edit</h1>

    {{ form_start(edit_form) }}
{#    {{ form_widget(edit_form) }}#}
    {{ form_end(edit_form) }}

    <a href="{{ path('parking_index') }}">Retour à la liste</a>
{% endblock %}
